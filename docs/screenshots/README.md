# Screenshots for the README

Drop PNGs here matching these names:
- main.png       — full window with banner + theme + status bar
- sftp.png       — SSH tab + SFTP side panel open
- profiles.png   — SSH profile editor showing tunnels/jump-hosts
- settings.png   — settings panel (any section)
- themes.png     — theme picker showing several theme options
- splits.png     — terminal with multiple panes split

PNG, 1600x1000 or similar; max ~500KB each (use https://squoosh.app to compress).
